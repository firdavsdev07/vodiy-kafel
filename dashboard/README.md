# Vodiy Kafel — Dashboard (admin panel)

Bu papka hali bo'sh — admin panel / dashboard uchun frontend keyinroq shu yerga ochiladi.

Boshqa loyihalar:
- `../api` — backend (NestJS), tashqi shartnoma: Swagger (`/api/docs`)
- `../storefront` — auth bo'lmagan (public) foydalanuvchilar ko'radigan sayt
