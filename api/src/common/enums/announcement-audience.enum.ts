/**
 * Xabar kimga (T-009) — ta'rifi `prisma/schema.prisma` da.
 *
 *   ALL      — yuboruvchi doirasidagi barcha faol mijozlar (menejerda —
 *              faqat o'ziga biriktirilganlar)
 *   SELECTED — tanlangan mijozlar
 */
export { AnnouncementAudience } from '../../prisma/prisma-client';
